package com.lotteryapp.repository;

public class UserDtoRepositoryShould {

    /* private FakeUserRepository fakeUserRepository;

    @BeforeEach
    public void initialise() {
        fakeUserRepository = new FakeUserRepository();
    }

    @Test
    public void create_and_store_a_user_registration() {
        UserDto userDto = new UserFixture().withUsername("a-username").build();
        fakeUserRepository.register(userDto);
        List<UserDto> userDtos = fakeUserRepository.allUsers();
        assertEquals(1, userDtos.size());
        assertEquals(userDto, userDtos.get(0));
    }

     */

}
